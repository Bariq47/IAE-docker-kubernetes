apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mysql-mahasiswa
  name: mysql-mahasiswa
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
